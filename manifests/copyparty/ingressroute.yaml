apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: copyparty-route
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`copyparty.alexbissessur.dev`)
    services:
    - name: copyparty
      port: 80
  tls:
    certResolver: le
