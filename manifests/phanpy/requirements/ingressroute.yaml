apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: phanpyroute
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`phanpy.alexbissessur.dev`)
    services:
    - name: phanpy
      port: 8080
  tls:
    certResolver: le
