apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: navidrome
spec:
  rules:
  - host: navidrome.alexbissessur.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: navidrome
            port:
              number: 80
