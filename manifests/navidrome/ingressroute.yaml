apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: naviroute
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`navidrome.alexbissessur.dev`)
    services:
    - name: navidrome
      port: 80
  tls:
    certResolver: le
