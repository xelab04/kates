apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ghostroute
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`immich.alexbissessur.dev`)
    services:
    - name: immich-server
      port: 2283
  tls:
    certResolver: le
