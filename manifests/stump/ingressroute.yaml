apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: naviroute
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`stump.alexbissessur.dev`)
    services:
    - name: stump
      port: 80
  tls:
    certResolver: le
