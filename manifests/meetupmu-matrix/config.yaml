apiVersion: v1
kind: ConfigMap
metadata:
  name: conduit-config
data:
  SERVER_NAME: "matrix.meetup.mu"
  conduit.toml: |
    [global]
    server_name = "matrix.meetup.mu"
    database_backend = "rocksdb"
    database_path = "/var/lib/matrix-conduit/"
    address = "0.0.0.0"
    port = 6167
    # Registration settings
    allow_registration = true
    registration_token = "some-secret"               # CHANGEME
    jwt_secret = "deadb33fc4f3c4f3deadb33fc4f3c4f3"  # CHANGEME: openssl rand -hex 16
    allow_check_for_updates = false
